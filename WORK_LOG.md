# Shall We Fight Prototype Work Log

## 개요

브라우저에서 바로 실행되는 HTML/CSS/JavaScript 기반 탑다운 2D 세력전 RPG/RTS 프로토타입을 구현했다.

- 기술 스택: HTML, CSS, JavaScript
- 외부 라이브러리 없음
- 파일 구조: `index.html`, `styles.css`, `scripts/*.js`
- 렌더링: Canvas API
- 게임 루프: `requestAnimationFrame`
- UI 기준 크기: 1600 x 760
- 캔버스 크기: 1450 x 640
- 맵 크기: 40 x 26 타일
- 타일 크기: 32px
- 월드 크기: 1280 x 832
- 실행 방식: `index.html`을 브라우저에서 직접 열기

## 핵심 클래스

- `Game`: 전체 게임 상태, 루프, 카메라, 승리/패배 조건, UI 갱신
- `GameMap`: 랜덤 타일 맵 생성, 타일 조회, A* 경로 탐색
- `Tile`: 지형 타입, 이동 가능 여부, 이동 비용, 방어 보너스
- `Unit`: 유닛 능력치, 이동, 공격, 자동 공격, 애니메이션 상태
- `Building`: 건물 점령, 자원/치료/생산/성채 점령 시간 처리
- `Faction`: 세력 이름, 색상, 자원
- `DialogueSystem`: 대화 및 회유 선택지 처리
- `AIController`: 적 세력 자동 행동
- `InputManager`: 마우스/키보드 입력 처리
- `Renderer`: 맵, 감시탑 범위, 건물, 유닛, 이펙트 렌더링
- `SpatialHash`: 유닛 충돌 처리용 공간 해시
- `MinHeap`: A* 경로 탐색용 최소 힙

## 파일 구조

- `index.html`: 게임 UI 마크업과 CSS/JS 파일 참조
- `styles.css`: 레이아웃, 패널, 버튼, 오버레이, 반응형 스케일 스타일
- `scripts/config.js`: 화면 스케일, 게임 상수, 세력/유닛 정의
- `scripts/utils.js`: 공통 유틸, 최소 힙, 공간 해시
- `scripts/map.js`: 타일과 랜덤 맵, A* 경로 탐색
- `scripts/entities.js`: 세력, 유닛, 건물
- `scripts/systems.js`: 대화, AI, 입력 처리
- `scripts/renderer.js`: Canvas 렌더링
- `scripts/main.js`: `Game` 클래스와 게임 시작

## 현재 구현 상태

### 맵과 카메라

- 60 x 40 사각형 타일 맵을 매 판 랜덤 생성한다.
- 큰 맵 전체를 한 화면에 축소해 보여주지 않고, 1450 x 640 캔버스를 월드 뷰포트로 사용한다.
- 기본 카메라 줌은 `0.75`로 낮춰 유닛과 건물이 작게 보이는 대신 더 넓은 월드 영역을 보여준다.
- 카메라는 `WASD`와 방향키로 수동 이동할 수 있다.
- 수동 이동 중에는 자동 추적을 잠시 멈춘다.
- 수동 이동이 끝나고 시간이 지나면 선택 유닛 또는 플레이어 유닛 중심으로 다시 따라간다.
- 지형 종류:
  - 평지
  - 숲: 이동 가능, 이동 속도 감소, 방어 보너스
  - 물: 이동 불가
  - 산: 이동 가능, 이동 속도 감소
- 풀, 꽃, 나무, 물결, 산 디테일을 타일에 그려 미니맵처럼 단조롭게 보이지 않도록 했다.

### 랜덤 배치

- 매 판마다 지형, 건물, 유닛 초기 스폰 위치가 랜덤 생성된다.
- 건물과 유닛이 같은 타일에 겹쳐 생성되지 않도록 예약 처리했다.
- 플레이어와 적 초기 위치가 너무 가깝지 않도록 최소 거리 조건을 둔다.
- 현재 건물 배치:
  - 병영 2개
  - 치료소 2개
  - 감시탑 2개
  - 금광 3개
  - 성채 1개
- 기존 고정 스폰 좌표와 고정 지형 보정 함수는 제거했다.

### 세력

- 전투 구도는 1:1에 중립 세력을 더한 구조다.
- 세력:
  - 플레이어: `푸른 연맹`, 파랑, `P`
  - 적 세력: `붉은 화영단`, 빨강, `A`
  - 중립: `회색 방랑자`, 회색, `N`
- 기존 적 B 세력은 제거했다.
- 중립 유닛은 자동 공격 대상에서 제외된다.

### 유닛

- 오리지널 소년 배틀 애니풍 캐릭터 콘셉트다.
- 유닛 타입:
  - 불꽃 검사
  - 바람 그림자
  - 번개 격투가
  - 물결 술사
  - 강철 방패병
- 각 유닛은 `id`, `name`, `kind`, `role`, `faction`, `hp`, `maxHp`, `attack`, `defense`, `moveSpeed`, `attackRange`, `persuasion`, `loyalty`, `x`, `y`, `target`, `state` 등을 가진다.
- 유닛별 내부 타입 `kind`로 외형과 장비를 분기한다.
- 타입별 실루엣:
  - 검사: 큰 검
  - 닌자: 두건과 표창
  - 격투가: 강조된 양손 주먹
  - 술사: 지팡이와 마법 구체
  - 방패병: 큰 방패와 창
- 유닛마다 세력 색상 링, 머리 위 배지, 체력바를 표시한다.

### 조작

- 좌클릭: 플레이어 유닛 선택
- 드래그: 다중 선택
- 우클릭 빈 타일: 이동
- 우클릭 적 유닛: 공격
- 우클릭 건물: 점령 이동
- `E`: 가까운 적/중립 캐릭터와 대화
- `ESC`: 선택 해제
- `WASD`/방향키: 카메라 이동

### 이동

- 플레이어 이동 명령은 A* 경로 탐색을 사용한다.
- 물 타일은 이동 불가다.
- 숲과 산에서는 이동 속도가 감소한다.
- 다중 선택 이동 시 첫 유닛은 클릭한 타일로 이동하고, 나머지는 주변 포메이션 타일로 퍼진다.
- 유닛끼리 겹치지 않도록 공간 해시 기반 충돌 밀어내기를 적용했다.

### 전투

- 공격 범위, 공격 쿨타임, 방어력, 지형 방어 보너스를 적용한다.
- HP가 0 이하가 되면 유닛은 제거된다.
- 선택 유닛 다중 공격 명령이 가능하다.
- 사거리 안에 적 전투 세력이 들어오면 자동 공격한다.
- 공격 적중 시 타격선, 피격 섬광, 충격 원, 데미지 숫자를 표시한다.
- 공격 시 유닛 몸과 장비가 대상 방향으로 튀어나가는 짧은 공격 애니메이션을 적용했다.

### 점령과 건물 효과

- 건물 주변에 현재 소유 세력이 아닌 유닛이 머무르면 점령 게이지가 증가한다.
- 점령 완료 시 건물 소유권과 색상이 해당 세력으로 변경된다.
- 건물 효과:
  - 병영: 일정 시간마다 gold를 사용해 유닛 자동 생산
  - 치료소: 주변 아군 회복
  - 감시탑: 점령 세력 색상의 반투명 시야 원 표시
  - 금광: gold 자동 생산
  - 성채: 플레이어 또는 적이 60초 장악하면 승패 조건에 반영
- 감시탑은 적 AI의 플레이어 탐지 반경을 늘리는 효과도 가진다.

### 대화와 회유

- 플레이어 유닛이 가까운 적/중립 캐릭터와 `E` 키로 대화할 수 있다.
- 선택지:
  - 우호적으로 설득
  - 강하게 압박
  - 보상을 제안
- 보상 제안은 gold 25를 소모하며, gold가 부족하면 버튼이 비활성화된다.
- 선택지에 따라 대상의 `loyalty`가 변한다.
- `loyalty`가 75 이상이면 대상이 플레이어 세력으로 합류한다.
- 중립은 회유 난이도가 낮고, 적은 더 어렵다.

### AI

- 적 세력 자동 행동을 구현했다.
- 행동 규칙:
  - 가까운 플레이어 유닛 발견 시 공격
  - 감시탑 보유 시 플레이어 탐지 반경 증가
  - 성채를 전략 목표로 우선 고려
  - 금광, 병영, 치료소, 감시탑, 성채 순으로 점령 목표 선정
  - 일정 시간마다 일부 유닛 랜덤 이동
  - 병영 점령 시 자동 유닛 생산

### UI

- 상단:
  - gold
  - 선택 유닛 수
  - 점령 건물 수
  - 성채 점령 시간
  - 게임 상태
- 오른쪽:
  - 선택 유닛 상세 정보
  - 건물 현황
  - 세력 범례
  - 조작 안내
- 하단:
  - 대화창
  - 명령 로그
- 오른쪽 상태 패널 폭은 150px로 줄였다.
- `#gameShell`은 `grid-template-columns: 1fr 150px` 구조를 사용한다.
- `#canvasWrap`과 캔버스 CSS 폭은 `100%`로 설정해 오른쪽 상태 패널을 제외한 남은 가로 공간을 모두 사용한다.
- 캔버스 내부 해상도는 `1450 x 640`으로 설정했다.
- 선택 유닛과 건물 패널은 해시 기반 dirty check로 필요한 경우에만 DOM을 재작성한다.
- 로그는 최근 4개만 유지한다.

### 화면 크기 대응

- 전체 게임 UI 기준 크기는 1600 x 760이다.
- 브라우저 창 크기에 맞춰 `--ui-scale` 값을 계산해 전체 UI를 비율 유지로 자동 확대/축소한다.
- `#gameFrame`은 실제 레이아웃 크기를 현재 스케일에 맞게 줄인다.
- `#gameShell`은 `transform-origin: top left` 기준으로 축소된다.
- 브라우저 주소창/모바일 UI 변화까지 반영하기 위해 `visualViewport`, `orientationchange`, `100dvh`를 사용해 스케일을 다시 계산한다.
- 오른쪽 상태 패널은 내부 스크롤을 사용하며, 긴 유닛/건물 텍스트는 말줄임 처리해 컨테이너를 밀어내지 않도록 했다.
- 캔버스 내부 해상도는 유지한다.

### 시작과 재시작 흐름

- 게임을 열자마자 시뮬레이션이 진행되지 않도록 시작 오버레이를 둔다.
- 최초 진입 시 `세력전 준비`와 `시작` 버튼을 표시한다.
- 시작 전에는 이동, 전투, AI, 점령, 자원 생산, 유닛 생산이 진행되지 않는다.
- 렌더링과 UI 표시는 유지되어 시작 전에도 맵과 초기 배치를 확인할 수 있다.
- 시작 버튼을 누르면 오버레이가 사라지고 게임 상태가 `교전 중`으로 바뀐다.
- 게임 종료 후에는 `재시작` 버튼으로 새 맵과 새 상태를 생성한다.

### 게임 템포

- `GAME_SPEED = 0.5`를 적용했다.
- 전체 시뮬레이션 템포를 절반으로 낮췄다.
- 이동, 공격 쿨타임, 점령, AI, 자원 생산, 유닛 생산, 애니메이션 진행이 함께 느려진다.

## 승리/패배 조건

- 승리:
  - 적 세력 유닛을 모두 제거
  - 또는 플레이어가 성채를 60초 동안 장악
- 패배:
  - 플레이어 유닛이 모두 사망
  - 또는 적이 성채를 60초 동안 장악

## 주요 수정 이력

- 고정 맵/고정 스폰 구조에서 랜덤 지형, 랜덤 건물, 랜덤 초기 스폰 구조로 변경
- 맵 크기를 30 x 20에서 60 x 40으로 확장
- 현재 플레이 테스트 밀도를 조정하기 위해 맵 크기를 60 x 40에서 30 x 20으로 축소한 뒤 40 x 26으로 재확장
- 큰 맵에 맞춰 카메라 이동과 자동 추적을 추가
- 클릭 지점과 이동 지점이 어긋나는 문제 수정
- 기본 카메라 줌을 0.75로 낮춰 맵을 더 넓게 볼 수 있도록 조정
- 작은 모니터에서 UI가 화면 밖으로 벗어나는 문제를 `#gameFrame`과 자동 스케일링으로 해결
- 화면 맞춤 로직을 `visualViewport`와 동적 뷰포트 높이 기준으로 보강
- 큰 화면에서 1배율 상한에 막히지 않고 컨테이너 폭/높이를 더 활용하도록 스케일 상한을 제거
- 오른쪽 상태 패널 콘텐츠가 컨테이너를 벗어나 보이는 문제를 내부 스크롤과 말줄임 처리로 보정
- 오른쪽 상태 패널 폭을 150px까지 줄이고, `#canvasWrap`이 남은 grid 영역 전체를 쓰도록 변경
- 캔버스 내부 해상도를 1450 x 640으로 넓혀 맵 표시 영역을 확장
- `index.html` 안에 있던 CSS와 JavaScript를 각각 `styles.css`, `game.js`로 1차 분리
- 단일 `game.js`를 기능별 스크립트(`scripts/config.js`, `utils.js`, `map.js`, `entities.js`, `systems.js`, `renderer.js`, `main.js`)로 추가 분리
- 게임 시작 버튼과 재시작 흐름 추가
- 세력 구분을 위해 유닛 링과 머리 위 배지 추가
- 게임 템포를 절반으로 감소
- 공격/피격 이펙트 추가
- 유닛 종류별 개성 있는 외형과 공격 애니메이션 추가
- 사거리 내 자동 공격 추가
- 2:1 구도가 부담스러워 적 세력을 하나로 축소
- 감시탑의 시각적 범위 표시와 AI 탐지 반경 보너스 추가
- 성채를 적이 장악해도 패배하도록 대칭 조건 추가
- UI 패널 갱신 비용을 줄이기 위해 dirty check 적용

## 실행 방법

1. `index.html` 파일을 브라우저에서 연다.
2. 별도 서버나 패키지 설치 없이 바로 실행된다.

## 검증

JavaScript는 분리된 `scripts/*.js` 파일을 개별 검사하고, 로드 순서대로 합친 코드도 `node --check`로 검사한다.

최근 확인 결과:

```text
node --check scripts/*.js
combined script syntax check
OK
```

현재 검증은 문법 검사 중심이다. 실제 플레이 품질을 보장하려면 랜덤 맵 연결성, 점령 전환, 대화 상태, 전투 추적, 종료 조건에 대한 시뮬레이션 검증을 추가해야 한다.

## 알려진 이슈와 보완 필요 사항

- 대화 시작 후 회유에 실패하거나 대화를 중단하는 경우 `talking` 상태가 풀리지 않아 유닛이 멈출 수 있다.
- 점령 중 다른 세력이 점령을 이어받을 때 기존 `captureProgress`가 그대로 이어져 불공정할 수 있다.
- 공격 대상 추적은 A*가 아니라 직선 이동을 사용하므로 전투 중 물 타일이나 장애 지형을 우회하지 못한다.
- 랜덤 맵 생성 후 플레이어, 적, 성채, 주요 건물이 같은 연결 영역에 있는지 보장하지 않는다.
- 시작 위치와 건물 배치가 간혹 불리하게 나올 수 있으므로 맵 밸런싱 검증이 필요하다.
- Fog of war와 플레이어 시야 시스템은 아직 없다.
- 감시탑은 AI 탐지 보너스와 시각적 범위 표시만 있으며, 플레이어 시야 시스템과는 연결되어 있지 않다.
- 병영 수동 생산 버튼은 없다.
- 유닛별 액티브 스킬은 없다.
- 건물/유닛 툴팁은 없다.
- 모바일 터치 조작은 지원하지 않는다.
- 일부 PowerShell/콘솔 환경에서 UTF-8 한글 출력이 깨져 보일 수 있다. 파일 자체는 UTF-8로 저장되어 있다.
## 최신 작업 내역

### Three.js 기반 탑다운 3D 렌더링 전환

- 기존 Canvas 2D 렌더러를 Three.js WebGL 렌더러로 교체했다.
- `index.html`에 Three.js CDN 로드를 추가했다.
- `scripts/renderer.js`는 `THREE.Scene`, `WebGLRenderer`, `OrthographicCamera` 기반으로 재작성했다.
- 기존 게임 로직 좌표계는 유지하고, 렌더링에서 월드 좌표 `x, y`를 Three.js `x, z` 평면으로 매핑했다.
- 마우스 좌표는 3D 지면 raycast로 변환하도록 `Renderer.screenToWorld()`를 추가했고, `Game.screenToWorld()`에서 이를 우선 사용하도록 연결했다.
- orthographic 카메라를 비스듬히 내려다보는 각도로 설정해 3D 탑다운 느낌을 만들었다.

### UI grid와 캔버스 비율 보정

- `#gameShell`의 grid 레이아웃을 복구했다.
- 캔버스 내부 해상도와 CSS 표시 크기가 어긋나 맵이 찌그러지는 문제를 수정했다.
- `#gameShell` grid를 `1450px 150px` / `48px 640px 72px`로 명시했다.
- `border` 대신 `outline`을 사용해 1600 x 760 내부 레이아웃 크기가 줄어들지 않도록 했다.
- `#canvasWrap`과 `canvas` 표시 크기를 1450 x 640으로 고정했다.
- `index.html`의 `#gameFrame` / `#gameShell` 들여쓰기 구조를 정리했다.

### 맵 크기 조정

- 맵 크기를 기존 60 x 40에서 30 x 20으로 줄인 뒤, 플레이 밀도를 조정하기 위해 40 x 26으로 재확장했다.
- 현재 월드 크기는 1280 x 832이다.
- 플레이어와 적 시작 위치 최소 거리를 새 맵 크기에 맞춰 18타일로 조정했다.

### 지형 디자인 개선

- 지형 타일에 색상 변화를 추가해 반복 격자 느낌을 줄였다.
- 평지에는 풀과 꽃 장식을 추가했다.
- 숲 타일에는 2단 나무 모델을 배치했다.
- 산 타일에는 높이감 있는 봉우리와 바위 장식을 추가했다.
- 물 타일은 반투명 재질과 물결 장식을 사용하도록 바꿨다.
- 조명은 HemisphereLight, DirectionalLight, rim light 조합으로 변경했다.
- tone mapping, shadow map, fog 설정을 추가해 장면 깊이감을 보강했다.

### 건물 디자인 개선

- 건물별 색상과 실루엣을 분리했다.
- 모든 건물에 받침대, 본체, 트림, 지붕, 세력 색상 배너를 추가했다.
- 병영은 방패와 무기 장식을 추가했다.
- 치료소는 십자 마크와 힐링 링을 추가했다.
- 감시탑은 전망대, 난간, 비콘을 추가했다.
- 금광은 광석, 입구, 수레를 추가했다.
- 성채는 추가 탑, 성벽, 깃발을 추가했다.
- 건물 주요 mesh에 검은 외곽선을 추가해 지형 위에서 더 잘 보이도록 했다.

### 유닛 디자인 개선

- 유닛을 단일 캡슐 형태에서 머리, 몸통, 가슴 장비, 팔, 다리, 어깨 장식 조합으로 변경했다.
- 병종별 장비를 추가했다.
  - 불꽃 검사: 검과 가드
  - 바람 그림자: 스카프와 단검
  - 번개 격투가: 격투 장갑
  - 물결 술사: 오브와 스태프
  - 강철 방패병: 방패와 무기
- 유닛 방향에 따라 모델이 회전하도록 `facingAngle()`을 추가했다.
- 유닛 몸통, 가슴, 머리에 검은 외곽선을 추가해 가독성을 높였다.
- 유닛 위 병종 라벨을 Three.js sprite 텍스처로 표시하도록 추가했다.
- 병종 라벨은 큰 흰색 글자와 반투명 어두운 배경판을 사용하도록 개선했다.

### 마우스 명령 이펙트 추가

- 우클릭 이동, 공격, 건물 점령 명령에 각각 다른 3D 이펙트를 추가했다.
- 이동 명령은 파란 링과 마커로 표시한다.
- 공격 명령은 빨간 링과 X 표시로 표시한다.
- 점령 명령은 링과 깃발 표시로 표현한다.
- `Game.commandEffects` 배열과 `addCommandEffect()`, `updateCommandEffects()`를 추가했다.
- 렌더러에서 `drawCommandEffect()`로 명령 이펙트를 그린다.

### 전투 중 이동 명령 수정

- 전투 중 이동 명령을 내려도 자동 공격이 즉시 다시 잡혀 이동이 취소되는 문제를 수정했다.
- `Unit.manualMove` 플래그를 추가했다.
- 수동 이동 중에는 자동공격 재획득을 막고, 목적지에 도착하면 `manualMove`를 해제한다.
- 공격 명령을 내리면 `manualMove`가 해제된다.

### 검증

- 변경 후 주요 스크립트에 대해 `node --check` 문법 검사를 수행했다.
- 확인한 파일:
  - `scripts/config.js`
  - `scripts/main.js`
  - `scripts/entities.js`
  - `scripts/renderer.js`

### 남은 개선 후보

- Three.js를 CDN에서 로드하므로 오프라인 실행 시 렌더러가 동작하지 않는다. 필요하면 로컬 vendor 파일로 포함해야 한다.
- 현재 동적 group은 매 프레임 재생성한다. 유닛 수가 늘어나면 mesh 재사용 또는 object pooling이 필요하다.
- 유닛 라벨 텍스처도 매 프레임 생성되므로, 병종별 라벨 캐시를 두면 성능이 좋아진다.
- 전투 추적 이동은 아직 A*가 아니라 직선 이동을 사용한다.
- 대화 상태 정리 로직은 아직 보완 필요하다.
